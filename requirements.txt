# 動作検証済みバージョンに固定（crewai は >= にすると未検証のメジャー更新を
# 引き込み「Invalid response from LLM call」等の挙動差が出るため == で固定する）
crewai==1.14.6
crewai-tools==1.14.6
pyyaml>=6.0
pydantic>=2.0
python-dotenv>=1.2.2
rich>=13.0
click>=8.1
ollama>=0.4
qdrant-client>=1.12
rank-bm25>=0.2.2
fastapi>=0.110
uvicorn[standard]>=0.27
# letta-client>=0.1   # memory.provider: letta を使う場合のみ必要
